# Rooms

## The Curse of Mount Clifton Manor

Short code: `CMCM`

The first MorseFlow escape room project is **The Curse of Mount Clifton Manor**.
Use `cmcm` as the suggested MQTT room slug when configuring Morseboards and
Node-RED topics for this room.

Use `docs/cmcm/` as the living implementation record for boards, prop ports,
MQTT topics, and test results.

Example topic root:

```text
morseflow/<site>/cmcm/<board_id>
```
